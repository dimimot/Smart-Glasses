# LM Studio (Qwen3-VL)
LM_STUDIO_BASE_URL=http://127.0.0.1:9094
LM_STUDIO_MODEL_NAME=Qwen3-VL-8B-Instruct-MLX-4bit

# Pipeline toggles (ON / OFF)
YOLO_ENABLED=OFF
TIME_LOGS=ON

# Optional overrides (uncomment to change defaults)
# MODEL=qwen3_vl        # qwen3_vl | llava | blip2 | blip_large
# PREPROC=opencv        # opencv | pillow
# DATA_DIR=
# YOLO_WEIGHTS=

